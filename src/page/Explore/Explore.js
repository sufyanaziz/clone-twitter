import React from "react";
import Layout from "../../components/Layout/HomeLayout";

const Explore = ({ history }) => {
  return (
    <Layout history={history} page="explore">
      <h1>Explore</h1>
    </Layout>
  );
};

export default Explore;
